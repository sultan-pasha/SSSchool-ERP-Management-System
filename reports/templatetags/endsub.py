# from django import template
# from datetime import datetime, timedelta

# register = template.Library()

# @register.filter
# def endsub(value, days):
#     try:
#         return value + timedelta(days=int(days))
#     except:
#         return value
